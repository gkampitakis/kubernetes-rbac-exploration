apiVersion: v1
kind: Pod
metadata:
  name: list-pod
spec:
  serviceAccountName: admin
  containers:
    - name: kubectl
      image: gkabitakis/kubernetes-rbac:latest
      args: ['list']
      imagePullPolicy: Always
  restartPolicy: Never